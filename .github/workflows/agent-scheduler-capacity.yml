name: Apply scheduler capacity optimization

on:
  pull_request:
    branches: [main]

permissions:
  contents: write

jobs:
  patch:
    if: github.head_ref == 'fix/scheduler-capacity-search' && github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/scheduler-capacity-search
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Apply prefix-sum and binary-search optimization
        run: |
          python3 <<'PY'
          from pathlib import Path
          import json

          helper = '''import type { ISODate } from '../types';

export interface DatedCapacity {
  date: ISODate;
  minutes: number;
}

/**
 * Finds the first calendar date whose cumulative non-negative capacity reaches
 * the requested amount. The sorted prefix is built once, so the lookup is
 * O(n log n) instead of rescanning every day while the finish date advances.
 */
export function earliestDateMeetingCapacity(
  capacityByDate: ReadonlyArray<DatedCapacity>,
  start: ISODate,
  initialFinish: ISODate,
  deadline: ISODate,
  requiredMinutes: number,
): ISODate {
  if (initialFinish >= deadline || requiredMinutes <= 0) return initialFinish >= deadline ? deadline : initialFinish;
  const days = capacityByDate
    .filter((day) => day.date >= start && day.date <= deadline)
    .map((day) => ({ date: day.date, minutes: Math.max(0, day.minutes) }))
    .sort((left, right) => left.date.localeCompare(right.date));
  const prefix = new Array<number>(days.length);
  let total = 0;
  for (let index = 0; index < days.length; index += 1) {
    total += days[index].minutes;
    prefix[index] = total;
  }
  let initialIndex = -1;
  let low = 0;
  let high = days.length;
  while (low < high) {
    const middle = Math.floor((low + high) / 2);
    if (days[middle].date <= initialFinish) low = middle + 1;
    else high = middle;
  }
  initialIndex = low - 1;
  if ((initialIndex >= 0 ? prefix[initialIndex] : 0) >= requiredMinutes) return initialFinish;
  if (total < requiredMinutes) return deadline;
  low = Math.max(0, initialIndex + 1);
  high = days.length - 1;
  while (low < high) {
    const middle = Math.floor((low + high) / 2);
    if (prefix[middle] >= requiredMinutes) high = middle;
    else low = middle + 1;
  }
  return days[low]?.date ?? deadline;
}

/**
 * Returns the first value from start + k * step that satisfies a monotonic
 * predicate. If none does before max, it preserves the legacy loop contract by
 * returning the first stepped value at or above max.
 */
export function minimumFeasibleSteppedValue(
  start: number,
  max: number,
  step: number,
  predicate: (value: number) => boolean,
): number {
  if (!(step > 0) || !Number.isFinite(step)) throw new Error('step must be a positive finite number');
  if (start >= max) return start;
  const maximumIndex = Math.max(0, Math.ceil((max - start) / step));
  const maximumCandidate = start + maximumIndex * step;
  if (!predicate(maximumCandidate)) return maximumCandidate;
  let low = 0;
  let high = maximumIndex;
  while (low < high) {
    const middle = Math.floor((low + high) / 2);
    if (predicate(start + middle * step)) high = middle;
    else low = middle + 1;
  }
  return start + low * step;
}
'''
          Path('src/lib/capacitySearch.ts').write_text(helper, encoding='utf-8')

          scheduler = Path('src/lib/schedulerV2.ts')
          source = scheduler.read_text(encoding='utf-8')
          import_marker = "import { ESTIMATE_POLICY, SCHEDULER_POLICY } from './schedulerPolicy';\n"
          assert import_marker in source
          source = source.replace(
              import_marker,
              import_marker + "import { earliestDateMeetingCapacity, minimumFeasibleSteppedValue } from './capacitySearch';\n",
              1,
          )
          old_capacity = '''  const capacityThrough = (end: ISODate) => input.capacityByDate
    .filter((day) => day.date >= input.start && day.date <= end)
    .reduce((sum, day) => sum + Math.max(0, day.minutes), 0);
  while (finish < input.deadline && capacityThrough(finish) < input.requiredMinutes) finish = addDays(finish, 1);
  return finish;'''
          new_capacity = '''  return earliestDateMeetingCapacity(
    input.capacityByDate,
    input.start,
    finish,
    input.deadline,
    input.requiredMinutes,
  );'''
          assert old_capacity in source
          source = source.replace(old_capacity, new_capacity, 1)
          old_cap = '''      let perDayTarget = Math.max(sessionMin, Math.ceil(averageStrictMinutes / 5) * 5);
      while (perDayTarget < maxAvailableBudget && !capCouldPossiblyFit(perDayTarget)) perDayTarget += 5;'''
          new_cap = '''      const loadStep = SCHEDULER_POLICY.balancedLoadStepMinutes;
      const initialPerDayTarget = Math.max(sessionMin, Math.ceil(averageStrictMinutes / loadStep) * loadStep);
      const perDayTarget = minimumFeasibleSteppedValue(
        initialPerDayTarget,
        maxAvailableBudget,
        loadStep,
        capCouldPossiblyFit,
      );'''
          assert old_cap in source
          source = source.replace(old_cap, new_cap, 1)
          scheduler.write_text(source, encoding='utf-8')

          test = '''import assert from 'node:assert/strict';
import { earliestDateMeetingCapacity, minimumFeasibleSteppedValue } from '../src/lib/capacitySearch';
import type { ISODate } from '../src/types';
import { addDays } from '../src/lib/date';

function legacyEarliest(
  capacityByDate: ReadonlyArray<{ date: ISODate; minutes: number }>,
  start: ISODate,
  initialFinish: ISODate,
  deadline: ISODate,
  requiredMinutes: number,
): ISODate {
  let finish = initialFinish;
  const capacityThrough = (end: ISODate) => capacityByDate
    .filter((day) => day.date >= start && day.date <= end)
    .reduce((sum, day) => sum + Math.max(0, day.minutes), 0);
  while (finish < deadline && capacityThrough(finish) < requiredMinutes) finish = addDays(finish, 1);
  return finish;
}

function legacyStepped(start: number, max: number, step: number, predicate: (value: number) => boolean): number {
  let value = start;
  while (value < max && !predicate(value)) value += step;
  return value;
}

const capacities = Array.from({ length: 365 }, (_, index) => ({
  date: addDays('2026-01-01', index),
  minutes: index % 7 === 0 ? 0 : index % 11 === 0 ? -30 : 45 + (index % 5) * 15,
}));
for (const initialOffset of [0, 7, 30, 90, 180]) {
  for (const required of [0, 60, 600, 4_000, 20_000, 99_999]) {
    const initial = addDays('2026-01-01', initialOffset);
    assert.equal(
      earliestDateMeetingCapacity(capacities, '2026-01-01', initial, '2026-12-31', required),
      legacyEarliest(capacities, '2026-01-01', initial, '2026-12-31', required),
      `prefix result mismatch at offset=${initialOffset}, required=${required}`,
    );
  }
}

for (const threshold of [0, 1, 5, 140, 198, 200, 201, 500]) {
  const predicate = (value: number) => value >= threshold;
  assert.equal(
    minimumFeasibleSteppedValue(140, 200, 5, predicate),
    legacyStepped(140, 200, 5, predicate),
    `binary search mismatch for threshold=${threshold}`,
  );
}

let calls = 0;
const result = minimumFeasibleSteppedValue(0, 100_000, 5, (value) => {
  calls += 1;
  return value >= 73_210;
});
assert.equal(result, 73_210);
assert.ok(calls <= 17, `binary search used too many predicate calls: ${calls}`);

const longCapacities = Array.from({ length: 20_000 }, (_, index) => ({
  date: addDays('2026-01-01', index),
  minutes: 30,
}));
const started = performance.now();
for (let index = 0; index < 100; index += 1) {
  earliestDateMeetingCapacity(longCapacities, '2026-01-01', '2026-01-01', addDays('2026-01-01', 19_999), 450_000);
}
const elapsed = performance.now() - started;
assert.ok(elapsed < 1_500, `prefix capacity lookup exceeded budget: ${elapsed.toFixed(1)}ms`);
console.log('✅ scheduler capacity prefix sum and monotonic binary search contracts passed');
'''
          Path('scripts/scheduler-capacity-search.test.ts').write_text(test, encoding='utf-8')

          package_path = Path('package.json')
          package = json.loads(package_path.read_text(encoding='utf-8'))
          command = 'vite-node scripts/scheduler-capacity-search.test.ts'
          current = package['scripts']['test:scheduler']
          if command not in current:
              package['scripts']['test:scheduler'] = current + ' && ' + command
          package_path.write_text(json.dumps(package, ensure_ascii=False, indent=2) + '\n', encoding='utf-8')

          Path('.github/workflows/agent-scheduler-capacity.yml').unlink()
          PY
          npm ci
          npm run typecheck
          npm run test:scheduler
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m "容量計算をprefix sumと二分探索へ最適化する"
          git push origin HEAD:fix/scheduler-capacity-search
